server:
  port: 8080

spring:
  application:
    name: dungeon-crawler-server
  datasource:
    url: jdbc:h2:mem:dungeoncrawler
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
