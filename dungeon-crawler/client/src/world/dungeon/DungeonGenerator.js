// Точка входа процедурной генерации: по seed выбирает биом и делегирует FloorGenerator'у.
export class DungeonGenerator {
  constructor(seed) {
    this.seed = seed;
  }

  generateFloor(depth) {
    // 1. выбрать биом по глубине (biomes/*.js)
    // 2. сгенерировать планировку (комнаты/пещеры/лабиринт)
    // 3. расставить монстров, лут, ловушки, торговцев, секретные комнаты
    // 4. расставить переходы между этажами (лестницы/лифты/шахты/верёвки)
  }
}
