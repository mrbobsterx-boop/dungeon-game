// Создаёт экземпляры предметов из data/items.json с учётом рандомизации характеристик.
export class ItemFactory {
  constructor(itemDefs) {
    this.defs = itemDefs;
  }

  create(itemId, rng) {
    // ...
  }
}
