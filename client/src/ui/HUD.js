// Постоянный игровой оверлей: портрет + уровень, полоски здоровья/маны,
// золото, ключи, топливо фонаря (%, тратится со временем — отдельно от
// предметов-масла в инвентаре, которые его пополняют), хотбар быстрых
// ячеек (1-0) и активные способности (Q/E).
export class HUD {
  render(ctx, player, hudState) {
    // hudState = { gold, keys, lanternFuelPercent, hotbar: [...], abilities: {Q, E} }
  }
}
