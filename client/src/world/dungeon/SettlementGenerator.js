// Генерация крупного безопасного поселения внутри подземелья: несколько
// NPC (торговцы разных специализаций, кузнец, квестодатели) на одной
// открытой площадке, в отличие от игрового лагеря (CampSystem) — поселение
// генерируется вместе с этажом и фиксировано, а не ставится игроком.
export class SettlementGenerator {
  static generate(floorGenerator, rng, depth) {
    // 1. выделить безопасную зону в планировке этажа (без монстров/ловушек)
    // 2. расставить NPC (entities/npc/Merchant.js, QuestGiver.js) по ролям
    // 3. декор: палатки/костры/баннеры/склады — визуально отличать от боевых зон
  }
}
